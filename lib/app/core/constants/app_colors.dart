import 'package:flutter/material.dart';

final class AppColors {
  const AppColors._();

  // Background
  static const background = Color(0xFF00101F);
  static const surface = Color(0xFF001628);
  static const surfaceVariant = Color(0xFF01162D);

  // Primary
  static const primary = Color(0xFF00A3FF);
  static const onPrimary = Colors.white;

  // Text
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white70;

  // Desktop environment (projects section)
  static const desktopSurface = Color(0xFF282C34);
  static const desktopTaskbar = Color(0xFF1A1D21);

  // Skill categories
  static const skillMobile = Color(0xFF42A5F5);
  static const skillFrontend = Color(0xFFFFA726);
  static const skillBackend = Color(0xFF4CAF50);
  static const skillDevOps = Color(0xFFAB47BC);
  static const skillDatabase = Color(0xFF26A69A);
}
